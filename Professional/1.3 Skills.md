# 1.3 Resiliency, Disaster Recovery & Scaling Architecture Skills

> **Domain Focus**: AWS Certified Solutions Architect – Professional (SAP-C02) & Cloud Resiliency Architecture  
> **Core Principle**: Section 1.3 represents an **integrated decision framework**:
> $$\text{Business Requirement} \longrightarrow \text{RTO / RPO} \longrightarrow \text{Auto-Recovery} \longrightarrow \text{Scaling Strategy} \longrightarrow \text{Backup Governance} \longrightarrow \text{Cost Optimization}$$

---

## 1. Unified Resiliency & Failure Decision Matrix

Every resiliency decision begins with identifying the **failure blast radius** and aligning architecture with verified business requirements.

```mermaid
flowchart TD
    FailureOrigin["Identify Failure Mode / Threat"] --> B1{"Component / Node Failure\n(EC2 / Container Crash)"}
    FailureOrigin --> B2{"Availability Zone Failure\n(Data Center Outage)"}
    FailureOrigin --> B3{"AWS Regional Outage\n(Catastrophic Loss)"}
    FailureOrigin --> B4{"Logical Corruption / Ransomware\n(Accidental Drop / Encrypted DB)"}
    FailureOrigin --> B5{"Account Compromise / Rogue Admin\n(Malicious Deletion)"}
    FailureOrigin --> B6{"Traffic Surge / Bottleneck\n(Spike in Ingestion)"}

    B1 --> S1["Self-Healing Architecture\n• ALB Health Checks\n• EC2 Auto Scaling Instance Replacement\n• CloudWatch Alarm Actions"]
    B2 --> S2["High Availability (HA)\n• Multi-AZ Subnets & ALBs\n• RDS Multi-AZ Synchronous Standby\n• Auto Scaling AZ Rebalancing"]
    B3 --> S3["Disaster Recovery (DR)\n• Route 53 / Global Accelerator Failover\n• Pilot Light / Warm Standby / Active-Active\n• Aurora Global DB / DynamoDB Global Tables"]
    B4 --> S4["Backup & PITR\n• Point-in-Time Recovery (RDS / DynamoDB)\n• S3 Versioning & S3 Object Lock\n• AWS Backup Immutable Recovery Points"]
    B5 --> S5["Isolated Security Architecture\n• Dedicated Central Backup Account\n• AWS Backup Vault Lock (Compliance Mode)\n• Cross-Account KMS CMKs & SCPs"]
    B6 --> S6["Dynamic Elastic Scaling\n• Scale-Out via EC2 Auto Scaling / ECS Fargate\n• Read Replicas / ElastiCache / DAX\n• DynamoDB On-Demand Capacity"]

    classDef origin fill:#7950f2,stroke:#5f3dc4,color:#ffffff;
    classDef branch fill:#1864ab,stroke:#0b5394,color:#ffffff;
    classDef sol fill:#2b8a3e,stroke:#1e632b,color:#ffffff;

    class FailureOrigin origin;
    class B1,B2,B3,B4,B5,B6 branch;
    class S1,S2,S3,S4,S5,S6 sol;
```

---

## 2. Skill 1: Designing DR Solutions Based on RTO and RPO

The exam evaluates whether you choose the **minimum necessary architecture** that satisfies recovery targets without incurring unnecessary cost.

```mermaid
flowchart LR
    subgraph DRSpectrum["The DR Cost vs. Recovery Progression"]
        direction LR
        BR["Backup & Restore\nRTO: Hours/Days\nRPO: Hours/Days\nCost: $"] --> DRS["AWS DRS\nRTO: Mins/Hours\nRPO: Seconds\nCost: $$"]
        DRS --> PL["Pilot Light\nRTO: 15m - 2h\nRPO: Sec/Mins\nCost: $$"]
        PL --> WS["Warm Standby\nRTO: Minutes\nRPO: Sub-Minute\nCost: $$$"]
        WS --> AA["Multi-Site Active/Active\nRTO: Near-Zero\nRPO: Real-Time\nCost: $$$$"]
    end

    classDef c1 fill:#2b8a3e,stroke:#1e632b,color:#ffffff;
    classDef c2 fill:#5c940d,stroke:#3b5b0c,color:#ffffff;
    classDef c3 fill:#e67700,stroke:#b05a00,color:#ffffff;
    classDef c4 fill:#d9480f,stroke:#9c3207,color:#ffffff;
    classDef c5 fill:#c92a2a,stroke:#861b1b,color:#ffffff;

    class BR c1;
    class DRS c2;
    class PL c3;
    class WS c4;
    class AA c5;
```

### DR Strategy Decision Matrix

| Requirement / Scenario | Recommended Strategy | Primary AWS Services | Cost / Overhead Profile |
| :--- | :--- | :--- | :---: |
| **Hours of downtime acceptable; lowest budget** | **Backup & Restore** | AWS Backup, S3, EBS Snapshots, CloudFormation | `$` (Lowest) |
| **Legacy on-premises VMs / physical servers requiring AWS DR** | **AWS DRS** | AWS Elastic Disaster Recovery, Staging EBS | `$$` (Low–Med) |
| **Data must stay synchronized, compute starts only on failure** | **Pilot Light** | RDS Cross-Region Replicas, Auto Scaling ($0 \rightarrow N$) | `$$` (Medium) |
| **Fast recovery ($< 15$ mins), running smaller standby fleet** | **Warm Standby** | Route 53, Auto Scaling ($10 \rightarrow 100$), Aurora Global DB | `$$$` (High) |
| **Zero downtime tolerance across AWS Regions** | **Multi-Site Active/Active** | Route 53 / Global Accelerator, DynamoDB Global Tables | `$$$$` (Highest) |

---

## 3. Skill 2: Implementing Architectures That Automatically Recover from Failure

Automatic recovery (**self-healing**) eliminates manual human intervention, reduces downtime, and prevents configuration error during incidents.

```mermaid
sequenceDiagram
    autonumber
    participant Users as End Users
    participant ALB as Application Load Balancer
    participant EC2_Bad as EC2 Instance A (Failing)
    participant EC2_Good as EC2 Instance B (Healthy)
    participant ASG as Auto Scaling Group
    participant CW as CloudWatch Alarms

    Users->>ALB: Send Inbound HTTP Requests
    ALB->>EC2_Bad: Periodic Health Check (e.g., HTTP 200 on /health)
    EC2_Bad--xALB: Timeout / HTTP 500
    Note over ALB: Mark Instance A as 'Unhealthy'
    ALB->>EC2_Good: Shift 100% Traffic to Healthy Instance B
    
    ALB->>ASG: Target Health Status = Unhealthy
    ASG->>EC2_Bad: Terminate Unhealthy Instance A
    ASG->>ASG: Launch Replacement Instance C from Launch Template
    ASG->>ALB: Register Instance C into Target Group
    Note over ALB,EC2_Good: System fully restored with ZERO manual intervention
```

### Self-Healing Architectural Elements
1. **ELB Target Group Health Checks**: Continuously polls target endpoints and instantly stops sending user traffic to unhealthy nodes.
2. **EC2 Auto Scaling Self-Healing**: Automatically terminates instances that fail EC2 status checks or ELB health checks and provisions fresh replacements from Launch Templates.
3. **RDS Multi-AZ Automated Failover**: Synchronously replicates to a standby replica in a second AZ; automatically modifies DNS endpoint records on primary failure ($< 60$s failover).
4. **CloudWatch Auto-Recovery Alarms**: Recovers underlying EC2 hardware degradation while preserving instance ID, private IP, and EBS volume attachments.

---

## 4. Skill 3: Scaling Strategies — Scale-Up vs. Scale-Out & Database Tiering

```mermaid
flowchart TD
    subgraph Scaling_Options["Scaling Architectural Patterns"]
        direction TB
        subgraph Vertical["Scale-Up (Vertical Scaling)"]
            V1["1 Single Instance (e.g., r6i.32xlarge)"]
            V2["• Legacy / monolithic codebases\n• Single-node relational DB engines\n• Huge in-memory computing\n❌ Single Point of Failure (SPOF)\n❌ Downtime during instance resizing"]
        end
        subgraph Horizontal["Scale-Out (Horizontal Scaling)"]
            H1["Multiple Stateless Instances behind ALB"]
            H2["• Elastic Auto Scaling Groups\n• Containerized microservices (ECS/EKS)\n• Fault isolated across 3+ AZs\n✅ High Availability & Self-Healing\n✅ Cost matches real-time traffic demand"]
        end
    end

    classDef vStyle fill:#d9480f,stroke:#9c3207,color:#ffffff;
    classDef hStyle fill:#2b8a3e,stroke:#1e632b,color:#ffffff;

    class Vertical,V1,V2 vStyle;
    class Horizontal,H1,H2 hStyle;
```

### Scaling Across Architecture Tiers

| Architecture Tier | Bottleneck / Requirement | Optimal AWS Scaling Mechanism |
| :--- | :--- | :--- |
| **Compute Tier (Stateless)** | Variable web/API traffic surges | **EC2 Auto Scaling / ECS Fargate** with Target Tracking Policies (CPU/ALB request count). |
| **Database Tier (Read Heavy)** | Heavy read queries overwhelming primary DB | **RDS Read Replicas** (up to 15) or **Aurora Auto Scaling Read Replicas**. |
| **Database Tier (Write Heavy)** | Massive transactional writes | **DynamoDB** (partition keys + on-demand capacity) or **Amazon Aurora Multi-Master**. |
| **Caching Tier** | Repeated database read queries | **Amazon ElastiCache (Redis/Memcached)** or **DynamoDB Accelerator (DAX)**. |
| **Legacy Monolithic DB** | Single relational DB requiring more compute | **Scale-Up** instance class (e.g., `db.r6g.large` $\rightarrow$ `db.r6g.16xlarge`) during maintenance window. |

---

## 5. Skill 4: Enterprise Data Backup, Restoration & Immutability

```mermaid
flowchart TD
    subgraph ProdAccount["Production AWS Account (Region 1)"]
        Workloads["Production EBS, RDS, DynamoDB, S3, EFS"]
        ProdBackup["AWS Backup (Daily Backup Plan)"]
        Workloads --> ProdBackup
    end

    subgraph CentralSecAccount["Dedicated Central Backup Account (Region 2 - DR)"]
        VaultLock["AWS Backup Vault Lock\n(Compliance Mode - Immutable)"]
        KMSKey["Customer Managed Key (KMS CMK)\n(Separate Account Key Policy)"]
        
        VaultLock --- KMSKey
    end

    ProdBackup == "Cross-Account & Cross-Region Copy" ==> VaultLock

    classDef prod fill:#1864ab,stroke:#0b5394,color:#ffffff;
    classDef secure fill:#c92a2a,stroke:#861b1b,color:#ffffff;

    class ProdAccount,Workloads,ProdBackup prod;
    class CentralSecAccount,VaultLock,KMSKey secure;
```

### Key Backup Governance Principles
- **Backup vs. Replication**: Replication protects against hardware and regional outage; Backups protect against accidental deletion, code corruption, and ransomware.
- **WORM Immutability**: Use **AWS Backup Vault Lock in Compliance Mode** or **S3 Object Lock** to prevent deletion by root users or compromised credentials.
- **Point-in-Time Recovery (PITR)**: Enable PITR on RDS, Aurora, and DynamoDB for continuous second-by-second recovery from application bugs.
- **Regular Restore Testing**: Backups must be validated via automated restore drills; untracked or untested backups fail audit compliance.

---

## 6. End-to-End Enterprise Resilient Production Architecture

The following reference diagram demonstrates how all Section 1.3 skills integrate into a production-grade, highly available, self-healing, and disaster-resilient system:

```mermaid
flowchart TD
    Users(["Global End Users"]) --> CF["Amazon CloudFront (Edge CDN)"]
    CF --> R53{"Route 53 DNS Failover\n(Latency / Health Checked)"}

    subgraph RegionA["Primary AWS Region (Active Production)"]
        R53 -- "Primary Endpoint" --> ALB_A["Application Load Balancer (Multi-AZ)"]
        
        subgraph AZ1["Availability Zone A"]
            EC2_1["EC2 / ECS Tasks"]
            RDS_Primary[("Aurora Primary (Writer)")]
        end
        
        subgraph AZ2["Availability Zone B"]
            EC2_2["EC2 / ECS Tasks"]
            RDS_Standby[("Aurora Replica (Reader)")]
        end
        
        ALB_A --> EC2_1 & EC2_2
        EC2_1 & EC2_2 --> RDS_Primary
        RDS_Primary -. "Synchronous AZ Sync" .-> RDS_Standby
        
        BackupA["AWS Backup Plan\n(Daily Snapshots)"]
        RDS_Primary & EC2_1 --> BackupA
    end

    subgraph RegionB["DR Region (Warm Standby / Pilot Light)"]
        R53 -. "Failover Endpoint" .-> ALB_B["Application Load Balancer"]
        ALB_B --> ASG_B["Auto Scaling Group\n(Capacity: 10% Warm or 0% Pilot)"]
        RDS_DR[("Aurora Global Database Secondary\n(Storage-Level Replication < 1s)")]
        ASG_B --> RDS_DR
    end

    subgraph CentralAccount["Dedicated Backup AWS Account"]
        LockedVault["AWS Backup Vault Lock\n(WORM Compliance Mode)"]
    end

    RDS_Primary == "Asynchronous Cross-Region Sync (<1s lag)" ==> RDS_DR
    BackupA == "Cross-Account & Cross-Region Backup Copy" ==> LockedVault

    classDef edge fill:#7950f2,stroke:#5f3dc4,color:#ffffff;
    classDef primary fill:#1864ab,stroke:#0b5394,color:#ffffff;
    classDef dr fill:#e67700,stroke:#b05a00,color:#ffffff;
    classDef vault fill:#c92a2a,stroke:#861b1b,color:#ffffff;

    class Users,CF,R53 edge;
    class ALB_A,EC2_1,EC2_2,RDS_Primary,RDS_Standby,BackupA primary;
    class ALB_B,ASG_B,RDS_DR dr;
    class CentralAccount,LockedVault vault;
```

---

## 7. Professional Exam Decision Cheat Sheet

```
Failure / Requirement Type                        Recommended AWS Architectural Decision
───────────────────────────────────────────────────────────────────────────────────────────────────
1. Single EC2 crash / degradation             ──► Auto Scaling self-healing + CloudWatch Auto-Recovery
2. AZ failure / Data center outage            ──► Multi-AZ ALB + Multi-AZ ASG + RDS Multi-AZ Standby
3. Regional outage (RTO: Hours, lowest cost)   ──► Backup & Restore (AWS Backup + S3 CRR + IaC)
4. Regional outage (RTO: < 15m, low budget)    ──► Warm Standby (10% compute fleet + Aurora Global DB)
5. Regional outage (RTO: Near-Zero)           ──► Multi-Site Active/Active (DynamoDB Global Tables)
6. On-premise VMs lift-and-shift DR to AWS    ──► AWS Elastic Disaster Recovery (AWS DRS)
7. Accidental DB DROP TABLE / data corruption ──► Point-in-Time Recovery (PITR)
8. Ransomware / Rogue Admin protection        ──► AWS Backup Vault Lock (Compliance Mode) in dedicated account
9. Heavy read traffic bottleneck               ──► RDS Read Replicas / ElastiCache Caching
10. Unpredictable HTTP traffic spikes          ──► EC2 Auto Scaling (Target Tracking on ALB Request Count)
```
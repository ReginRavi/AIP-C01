# Task 4.3: Determine the Optimal Compute and Application Strategy — Containers (Amazon ECS, Amazon EKS, AWS Fargate, Amazon ECR)

> [!NOTE]
> **Exam Mindset**: For the AWS Solutions Architect Professional (SAP-C02) and AI Practitioner (AIP-C01) exams, do not view ECS, EKS, Fargate, and ECR as four isolated services. They form a single integrated container execution engine:
> - **Amazon ECR (Image Storage)** = *"Where do I store, version, and scan private Docker container images securely?"*
> - **Amazon ECS (AWS-Native Orchestration)** = *"How do I orchestrate container tasks seamlessly using deep AWS service integrations with minimal operational complexity?"*
> - **Amazon EKS (Managed Kubernetes)** = *"How do I run standardized Kubernetes manifests, Helm charts, and custom operators on AWS?"*
> - **AWS Fargate (Serverless Compute)** = *"How do I run ECS/EKS container workloads without provisioning, patching, or scaling EC2 host servers?"*

---

## 1. End-to-End Container Architecture & Ecosystem Pipeline

```mermaid
flowchart TD
    subgraph Developer_Build ["1. Build & Package Phase"]
        Dev["Developer Application Code"] ==> Dockerfile["Build Container Image<br/>(Application + Libraries + Runtime)"]
    end

    subgraph Storage_Phase ["2. Image Registry & Security (ECR)"]
        Dockerfile ==>|"docker push (TLS / IAM Auth)"| ECR["Amazon ECR<br/>• Private Container Registry<br/>• Automated Image Vulnerability Scanning<br/>• Image Lifecycle & Retention Policies"]
    end

    subgraph Orchestration_Phase ["3. Container Orchestration Engine"]
        ECR ==>|"Pull Image"| Orch_Choice{"Select Container Orchestrator"}
        Orch_Choice -- "AWS-Native Simplicity" --> ECS["Amazon ECS<br/>• Task Definitions & Services<br/>• Native ALB / IAM Integration"]
        Orch_Choice -- "Kubernetes Standard / CNCF" --> EKS["Amazon EKS<br/>• Managed Kubernetes Control Plane<br/>• Helm / kubectl / Operators"]
    end

    subgraph Compute_Phase ["4. Compute Execution Layer"]
        ECS & EKS ==> Compute_Choice{"Who Manages the Infrastructure?"}
        Compute_Choice -- "Serverless (Zero Host Management)" --> Fargate["AWS Fargate Compute Engine<br/>• vCPU / RAM Billing per Second<br/>• Zero OS Patching or AMIs"]
        Compute_Choice -- "Full Control / Predictable Capacity" --> EC2_Fleet["Amazon EC2 Host Fleet<br/>• Custom AMIs, GPUs, Placement Groups<br/>• Managed ASG Capacity Providers"]
    end

    subgraph Application_Delivery ["5. Delivery & Ingress Layer"]
        Fargate & EC2_Fleet ==> ALB["Application Load Balancer (ALB)"]
        ALB ==> EndUser["End User / Client Requests"]
    end

    style Developer_Build fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style Storage_Phase fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Orchestration_Phase fill:#d4edda,stroke:#28a745,stroke-width:2px
    style Compute_Phase fill:#f8d7da,stroke:#dc3545,stroke-width:1px
    style Application_Delivery fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
```

---

## 2. Containers vs. Virtual Machines (VMs) Core Fundamentals

### 2.1 Virtual Machine vs. Container Architecture Comparison

```mermaid
flowchart TD
    subgraph VM_Arch ["Virtual Machine (Hardware Virtualization)"]
        App1["Application A"] --- OS1["Guest OS (Linux/Windows)<br/>• Kernel & System Binaries"]
        App2["Application B"] --- OS2["Guest OS (Linux/Windows)<br/>• Kernel & System Binaries"]
        OS1 & OS2 --- Hypervisor["Hypervisor (Type 1 / Type 2)"]
        Hypervisor --- HostHW["Host Hardware & Operating System"]
    end

    subgraph Container_Arch ["Container (OS Virtualization)"]
        ContA["Container A<br/>(App A + Libs)"] --- Engine["Docker / Container Runtime"]
        ContB["Container B<br/>(App B + Libs)"] --- Engine
        Engine --- SharedKernel["Shared Host OS Kernel"]
        SharedKernel --- HostHW2["Host Hardware"]
    end

    style VM_Arch fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Container_Arch fill:#d4edda,stroke:#28a745,stroke-width:2px
```

### 2.2 Core Comparison Matrix: VMs vs. Containers

| Dimension | Virtual Machines (EC2) | Containers (ECS / EKS) |
| :--- | :--- | :--- |
| **Virtualization Layer** | Hardware level (Hypervisor / Guest OS) | OS level (Shared Host Kernel) |
| **Isolation Level** | Hard VM-level boundary (Separate OS per VM) | Process-level isolation (Namespaces & cgroups) |
| **Startup Time** | Minutes (Full OS boot cycle) | Seconds (Process instantiation) |
| **Resource Overhead** | High (Requires dedicated RAM/CPU for Guest OS) | Minimal (Shared kernel, lightweight image footprint) |
| **Portability** | Restricted by OS environment & dependencies | Universal ("Build once, run anywhere" container image) |
| **Primary Use Case** | Monoliths, legacy OS requirements, raw kernel control | Microservices, rapid scaling fleets, event batch tasks |

---

## 3. Amazon ECR (Elastic Container Registry) Deep Dive & Image Lifecycle

```mermaid
flowchart TD
    subgraph CI_CD_Pipeline ["CI/CD Build Pipeline"]
        CodeBuild["AWS CodeBuild / GitHub Actions"] ==>|"1. Build & Tag Image"| TaggedImage["Image: myapp:v2.1"]
        TaggedImage ==>|"2. Authenticate (aws ecr get-login-password)"| ECR_Auth["ECR Authentication via IAM"]
    end

    subgraph ECR_Repository ["Amazon ECR Registry"]
        ECR_Auth ==> Push["3. Push Image to ECR Repository"]
        Push ==> Scan["4. Automated Vulnerability Scanning<br/>(Basic Scanning / Amazon Inspector)"]
        Push ==> Lifecycle["5. Evaluate Lifecycle Policy<br/>(e.g., Keep last 20 tagged images, expire untagged > 14 days)"]
        Push ==> Immutable["6. Enforce Image Tag Immutability<br/>(Prevents overwriting production tags)"]
    end

    subgraph Target_Compute ["Deployment Target"]
        Scan & Lifecycle ==> Pull["7. Pull Verified Image via IAM Task Execution Role"]
        Pull ==> ECS_Tasks["ECS Tasks / EKS Pods"]
    end

    style CI_CD_Pipeline fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style ECR_Repository fill:#d4edda,stroke:#28a745,stroke-width:2px
    style Target_Compute fill:#fff3cd,stroke:#ffc107,stroke-width:1px
```

> [!IMPORTANT]
> **ECR Image Tag Immutability & Lifecycle Policies for SAP-C02**:
> - **Tag Immutability**: Enable Tag Immutability on production repositories to prevent malicious or accidental overwriting of existing tags (e.g., `myapp:latest` or `myapp:v1.0`).
> - **Lifecycle Policies**: Use ECR Lifecycle Policies to automatically purge old or untagged container images, eliminating uncontrolled storage cost accumulation.

---

## 4. Amazon ECS (Elastic Container Service) Architecture

### 4.1 ECS Component Hierarchy: Task Definition $\rightarrow$ Task $\rightarrow$ Service

```mermaid
flowchart TD
    subgraph Task_Definition ["Task Definition (Blueprint JSON)"]
        TD["ECS Task Definition<br/>• Container Image URL (ECR)<br/>• CPU & Memory Allocation<br/>• Container Port Mappings<br/>• IAM Roles (Task Role & Execution Role)<br/>• Log Configuration (awslogs CloudWatch)<br/>• Environment Variables / Secrets Manager"]
    end

    subgraph ECS_Cluster ["Amazon ECS Cluster"]
        subgraph ECS_Service ["ECS Service (Desired State Maintainer)"]
            Service_Ctrl["Service Scheduler<br/>• Desired Count = 3<br/>• Target Group Health Checks<br/>• Deployment Strategy (Rolling / Circuit Breaker)"]
            
            Service_Ctrl ==> Task1["ECS Task 1 (Container Instance)"]
            Service_Ctrl ==> Task2["ECS Task 2 (Container Instance)"]
            Service_Ctrl ==> Task3["ECS Task 3 (Container Instance)"]
        end
    end

    TD ==>|"Instantiates"| ECS_Service

    style Task_Definition fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style ECS_Cluster fill:#d4edda,stroke:#28a745,stroke-width:2px
    style ECS_Service fill:#fff3cd,stroke:#ffc107,stroke-width:1px
```

---

### 4.2 ECS Task Execution Role vs. ECS Task Role (Critical Exam Distinctions)

```mermaid
flowchart TD
    subgraph ECS_Task_Boundary ["ECS Task Boundary"]
        subgraph Exec_Role ["1. Task Execution Role (Infrastructure Role)"]
            Exec_IAM["IAM Role: ecsTaskExecutionRole"]
            Exec_IAM ==> PullECR["Pull Images from Amazon ECR"]
            Exec_IAM ==> FetchLogs["Write Logs to CloudWatch Logs"]
            Exec_IAM ==> FetchSecrets["Fetch Secrets from AWS Secrets Manager / SSM"]
        end

        subgraph Task_Role ["2. Task Role (Application Role)"]
            App_IAM["IAM Role: myAppTaskRole"]
            App_IAM ==> ReadDynamo["Read/Write to Amazon DynamoDB"]
            App_IAM ==> ReadS3["Read Objects from Amazon S3"]
            App_IAM ==> SendSQS["Publish Messages to Amazon SQS"]
        end
    end

    style Exec_Role fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Task_Role fill:#d4edda,stroke:#28a745,stroke-width:2px
```

| Role Type | Who Uses It? | Primary Purpose | Standard Granted Permissions |
| :--- | :--- | :--- | :--- |
| **Task Execution Role** (`executionRoleArn`) | The **ECS Container Agent** (Infrastructure) | Enables the container agent to launch the container task | `ecr:GetDownloadUrlForLayer`, `logs:CreateLogStream`, `secretsmanager:GetSecretValue` |
| **Task Role** (`taskRoleArn`) | The **Application Code** running inside the container | Grants the application permissions to call AWS APIs | `dynamodb:PutItem`, `s3:GetObject`, `sqs:SendMessage` |

---

## 5. Amazon EKS (Elastic Kubernetes Service) Architecture

```mermaid
flowchart TD
    subgraph EKS_Control_Plane ["AWS Managed Control Plane (High Availability Across 3 AZs)"]
        K8s_API["Kubernetes API Server"]
        etcd["etcd Database (State Storage)"]
        Controller["Kubernetes Controller Manager"]
        K8s_API --- etcd & Controller
    end

    subgraph Compute_Worker_Nodes ["Data Plane (Worker Compute Options)"]
        subgraph Managed_Node_Groups ["1. Managed Node Groups (EC2)"]
            MNG["EC2 Worker Instances<br/>• Managed by AWS ASG<br/>• Automated OS updates"]
        end

        subgraph Fargate_Profiles ["2. EKS on AWS Fargate"]
            Fargate_Pods["Serverless Pods<br/>• 1 Pod = 1 Dedicated VM Isolation<br/>• vCPU/RAM per Pod billing"]
        end

        subgraph Karpenter_Scaling ["3. Karpenter Dynamic Autoscaler"]
            Karpenter["Karpenter Controller<br/>• Right-sized EC2 instance provisioning<br/>• Sub-minute node provisioning"]
        end
    end

    K8s_API ==>|"kubectl / Helm API Commands"| MNG & Fargate_Pods & Karpenter

    style EKS_Control_Plane fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style Compute_Worker_Nodes fill:#d4edda,stroke:#28a745,stroke-width:2px
```

> [!NOTE]
> **EKS Operational & Financial Realities for SAP-C02**:
> - **Control Plane Fee**: Amazon EKS charges a fixed control plane fee of **$0.10/hour (~$73/month)** per cluster.
> - **Operational Complexity**: EKS provides native Kubernetes API compatibility, but requires managing Kubernetes constructs (Pods, Deployments, CRDs, Helm charts, RBAC, Ingress Controllers). Select EKS when existing Kubernetes manifests or multi-cloud CNCF standardization is explicitly required.

---

## 6. AWS Fargate Serverless Compute Engine vs. EC2 Launch Type

```mermaid
flowchart TD
    Start_Compute(["Choose Container Compute Launch Type"]) --> Q_Host{"Do you need host OS access, custom kernel drivers, or specialized GPUs?"}

    Q_Host -- "YES (Host OS Control / GPU / Lowest Unit Cost at Scale)" --> Launch_EC2["ECS / EKS on Amazon EC2<br/>• You manage EC2 instances, AMIs, OS patching<br/>• Supports Placement Groups & GPU instances<br/>• Managed via ECS Capacity Providers / EKS Node Groups"]

    Q_Host -- "NO (Serverless / Zero Infrastructure Maintenance)" --> Launch_Fargate["ECS / EKS on AWS Fargate<br/>• AWS manages underlying host infrastructure<br/>• Pay per vCPU & RAM per second<br/>• Zero OS patching or AMI maintenance"]

    style Launch_EC2 fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Launch_Fargate fill:#d4edda,stroke:#28a745,stroke-width:2px
```

### 6.1 ECS on EC2 vs. ECS on Fargate Feature Matrix

| Feature / Dimension | ECS on Amazon EC2 | ECS on AWS Fargate |
| :--- | :--- | :--- |
| **Server Management** | High (Customer manages EC2 instances, OS patching, AMIs) | **Zero** (AWS manages host infrastructure entirely) |
| **Pricing Model** | Pay for provisioned EC2 instance capacity (24/7 runtime) | Pay for exact **vCPU and Memory** consumed per second by tasks |
| **Networking Mode** | Supports `awsvpc`, `bridge`, `host`, `none` | Supports **`awsvpc` ONLY** (Task receives dedicated ENI) |
| **Host-Level Access / SSH**| Full root access to underlying EC2 host | **No access** to underlying host OS |
| **GPU / Specialized HW** | Supported (NVIDIA GPU, AWS Inferentia / Trainium) | Not supported |
| **AWS Outposts Support** | Fully Supported | **❌ NOT Supported on AWS Outposts** |
| **Daemon Services** | Supported (Runs 1 container task per EC2 instance) | Not supported |

---

## 7. Container Networking, Security & Storage Architectures

### 7.1 ECS Networking Modes (`awsvpc` vs. `bridge` vs. `host`)

```mermaid
flowchart TD
    subgraph awsvpc_Mode ["1. awsvpc Mode (Recommended & Fargate Requirement)"]
        TaskA["ECS Task A"] --- ENI1["Dedicated VPC ENI 1<br/>(Private IP: 10.0.1.50)"]
        TaskB["ECS Task B"] --- ENI2["Dedicated VPC ENI 2<br/>(Private IP: 10.0.1.51)"]
        ENI1 & ENI2 --- Subnet["VPC Subnet Security Groups"]
    end

    subgraph bridge_Mode ["2. bridge Mode (Docker Default on EC2)"]
        Cont1["Container 1"] & Cont2["Container 2"] --- DockerBridge["Docker Virtual Bridge"]
        DockerBridge --- HostENI["EC2 Host ENI (Dynamic Port Mapping)"]
    end

    style awsvpc_Mode fill:#d4edda,stroke:#28a745,stroke-width:2px
    style bridge_Mode fill:#fff3cd,stroke:#ffc107,stroke-width:1px
```

---

### 7.2 Persistent Storage Options for Container Workloads

```mermaid
flowchart TD
    Start_Storage(["Container Storage Requirement"]) --> Q_Store{"What type of data must the container access?"}

    Q_Store -- "Ephemeral Task Data (Log buffers, temporary scratch)" --> Ephemeral["Task Ephemeral Storage<br/>• Up to 200 GB on Fargate<br/>• Local EC2 EBS volume"]

    Q_Store -- "Shared Persistent File Storage Across Tasks" --> EFS["Amazon EFS Volume Attachment<br/>• Native ECS / Fargate EFS volume support<br/>• Multi-AZ concurrent read/write for CMS / stateful apps"]

    Q_Store -- "High-IOPS Persistent Block Storage (Single Task)" --> EBS_Attach["Amazon EBS Volume Attachment<br/>• Dedicated EBS volume attached to single ECS Task"]

    Q_Store -- "Unstructured Objects / Media Files" --> S3_SDK["Amazon S3 via AWS SDK / S3 Express<br/>• Accessed using IAM Task Role credentials"]

    style Ephemeral fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style EFS fill:#d4edda,stroke:#28a745,stroke-width:2px
    style EBS_Attach fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style S3_SDK fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
```

---

## 8. Real-World Exam Scenarios & Strategic Decision Patterns

### Scenario 1: AWS-Native Containerization with Zero Infrastructure Maintenance
- **Scenario**: A financial organization wants to migrate 40 microservices to Docker containers. The team has no Kubernetes experience, wants to eliminate server management, and requires full integration with Application Load Balancers and AWS IAM.
- **Solution**: Deploy containers to **Amazon ECS using the AWS Fargate launch type**.
- **Reasoning**:
  - Amazon ECS provides zero control-plane cost and seamless AWS integration (IAM, ALB, CloudWatch).
  - AWS Fargate removes all EC2 management, OS patching, and capacity planning.
  - *Exam Traps*: Do not select EKS when Kubernetes expertise is lacking and AWS-native operational simplicity is requested.

---

### Scenario 2: Existing Kubernetes Migration with Zero Control Plane Management
- **Scenario**: A company is migrating an enterprise workload already packaged with Helm charts and Kubernetes manifests from an on-premises vSphere cluster to AWS. The operations team insists on maintaining standard Kubernetes tooling while eliminating control plane management.
- **Solution**: Migrate the workload to **Amazon EKS with EKS Managed Node Groups (or Fargate)**.
- **Reasoning**:
  - Amazon EKS preserves existing Kubernetes manifests, kubectl scripts, and Helm charts without code rewrite.
  - AWS handles control plane availability, etcd backups, and master node patching.
  - *Exam Traps*: Re-architecting existing Kubernetes apps onto Amazon ECS causes unnecessary operational overhead and migration delays.

---

### Scenario 3: Container Microservices Requesting Database Credentials Privately
- **Scenario**: A containerized application running on ECS Fargate needs to query Amazon Aurora PostgreSQL securely. Database credentials must be rotated automatically and cannot be hardcoded in container images or environment text files.
- **Solution**: Store database credentials in **AWS Secrets Manager**, inject them into the ECS Task Definition via `secrets` parameter using the **Task Execution Role**, and grant the application's **Task Role** database access permissions.
- **Reasoning**:
  - `secrets` injection decrypts credentials at runtime without storing them in the image.
  - Secrets Manager supports automated credential rotation via Lambda functions.
  - *Exam Traps*: Hardcoding secrets or storing plain-text credentials in container environment variables violates security compliance.

---

### Scenario 4: High-Performance GPU AI/ML Model Training Containers
- **Scenario**: An AI startup runs intensive deep learning container training jobs requiring specialized NVIDIA GPUs and custom Linux kernel tuning. Cost optimization for heavy 24/7 compute is essential.
- **Solution**: Use **Amazon ECS (or EKS) with the Amazon EC2 Launch Type**, leveraging GPU-optimized instance families (`g5` / `p4`) and **ECS Capacity Providers with Auto Scaling Groups**.
- **Reasoning**:
  - AWS Fargate does not support GPU hardware or host kernel tuning.
  - EC2 launch type allows selecting GPU instances and applying Savings Plans / Reserved Instances for predictable baseline compute.
  - *Exam Traps*: Selecting AWS Fargate for GPU workloads is an invalid configuration.

---

## 9. SAP-C02 / AIP-C01 Master Container Architectural Decision Engine

```mermaid
flowchart TD
    Start(["Container Architecture Decision Task"]) --> Q1{"What is the primary requirement?"}

    Q1 -- "Store, version, and scan private Docker container images" --> Ans_ECR["Amazon ECR<br/>(Enforce Tag Immutability & Lifecycle Policies)"]

    Q1 -- "Orchestrate containers with AWS-native simplicity" --> Q_Compute{"Who manages the host compute servers?"}
    Q_Compute -- "AWS (Serverless)" --> Ans_ECSFargate["Amazon ECS on AWS Fargate"]
    Q_Compute -- "Customer (Host OS / GPU / Custom Kernel)" --> Ans_ECSEC2["Amazon ECS on Amazon EC2"]

    Q1 -- "Orchestrate containers requiring Kubernetes / Helm" --> Q_K8sCompute{"Who manages Kubernetes worker nodes?"}
    Q_K8sCompute -- "AWS (Serverless Pods)" --> Ans_EKSFargate["Amazon EKS on AWS Fargate"]
    Q_K8sCompute -- "Customer (EC2 Worker Groups)" --> Ans_EKSEC2["Amazon EKS + Managed Node Groups / Karpenter"]

    Q1 -- "Deploy web container app directly from Git repo with zero orchestration config" --> Ans_AppRunner["AWS App Runner"]

    Q1 -- "Shared stateful filesystem across container tasks" --> Ans_EFS["Attach Amazon EFS Volumes to ECS/EKS Tasks"]

    style Ans_ECR fill:#d4edda,stroke:#28a745,stroke-width:2px
    style Ans_ECSFargate fill:#d4edda,stroke:#28a745,stroke-width:2px
    style Ans_ECSEC2 fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Ans_EKSFargate fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
    style Ans_EKSEC2 fill:#fff3cd,stroke:#ffc107,stroke-width:1px
    style Ans_AppRunner fill:#d4edda,stroke:#28a745,stroke-width:2px
    style Ans_EFS fill:#d1ecf1,stroke:#17a2b8,stroke-width:1px
```

---

## 10. Top Exam Traps & Anti-Patterns Checklist

> [!WARNING]
> **Critical Exam Traps for Container Services**:
> 1. **Amazon ECR is an Image Registry, NOT a Compute Runtime**: ECR stores, scans, and manages container images. It cannot execute container tasks.
> 2. **Confusing ECS Task Execution Role vs. Task Role**:
>    - **Task Execution Role**: Used by the ECS agent to pull images from ECR and write logs to CloudWatch.
>    - **Task Role**: Used by the application code inside the container to access AWS services (S3, DynamoDB, SQS).
> 3. **AWS Fargate is NOT Supported on AWS Outposts**: Outposts requires EC2 host capacity for running ECS or EKS tasks.
> 4. **Selecting EKS when Kubernetes is NOT Required**: If the user asks for simple, AWS-native container management without mentioning Kubernetes or Helm, select **Amazon ECS** to avoid unnecessary $0.10/hr control plane costs and operational complexity.
> 5. **AWS Fargate Does NOT Support GPUs or Host Privilege Mode**: For workloads requiring NVIDIA GPUs, custom Linux kernel parameters, or daemon instances, choose **ECS/EKS on EC2**.
> 6. **Using Hardcoded Credentials in Container Images**: Always fetch credentials dynamically from **AWS Secrets Manager** or **SSM Parameter Store** using the container's Task Role.
> 7. **Forgetting ECR Lifecycle Policies**: Without lifecycle policies, accumulating Docker image tags leads to continuously expanding storage costs. Always define policies to retain only the latest $N$ tags or expire untagged images.
